# Postgres connection string (Supabase: Project Settings -> Database -> Connection string -> URI)
DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Session secret - set to a long random string in production
SESSION_SECRET=change_this_to_a_random_secret_string

# HelloSign / Dropbox Sign API key (from https://app.hellosign.com/home/myAccount#api)
HELLOSIGN_API_KEY=

# If left blank above, the app runs in MOCK SEND mode: agreements are logged
# and marked "Sent (mock)" instead of actually calling HelloSign. This lets
# your team test the whole flow before the real API key is added.

# Gmail account Offer Letters are emailed from (sylvesterhagan93@gmail.com).
# GMAIL_APP_PASSWORD is a 16-character "App Password" generated in Google
# Account -> Security -> App Passwords (requires 2-Step Verification to be
# turned on first) - NOT the account's real login password.
GMAIL_USER=
GMAIL_APP_PASSWORD=

# If either is left blank above, Offer Letters run in MOCK SEND mode: the
# proposal PDF still generates so the team can see exactly what would be
# sent, but no real email goes out.

PORT=3000
